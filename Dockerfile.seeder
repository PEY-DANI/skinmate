FROM python:3.12-slim

WORKDIR /app

# pyproject.toml 및 소스코드 복사 (pip install 시 src 디렉토리 필요)
COPY pyproject.toml /app/
COPY src/ /app/src/

RUN pip install --no-cache-dir .

# 구동 스크립트 복사
COPY scripts/ /app/scripts/

# 실행 명령
CMD ["python", "scripts/populate_graph.py"]
